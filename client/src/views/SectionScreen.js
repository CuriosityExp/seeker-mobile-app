import React from "react";
import {
  Image,
  Text,
  View,
  TouchableOpacity,
  ScrollView,
  StyleSheet,
} from "react-native";

export default function SectionScreen({ navigation }) {
  return <Text>P</Text>;
}
